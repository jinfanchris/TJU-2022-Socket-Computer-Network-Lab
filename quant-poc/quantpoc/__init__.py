"""quantpoc — a small quantitative-trading POC engine.

One engine, two thin clients (a React web app and a Textual TUI) talk to it
over the same FastAPI REST + WebSocket API.
"""

__version__ = "0.1.0"
